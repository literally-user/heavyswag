[project]
name = "heavyswag"
version = "0.1.0"
description = "The hype python web framework"
requires-python = ">=3.12"
dependencies = []

[tool.uv.build-backend]
module-root = "src"

[dependency-groups]
